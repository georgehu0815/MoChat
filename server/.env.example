# MoChat Server Configuration

# Server Port
PORT=3000

# CORS Configuration
CORS_ORIGIN=*

# Socket.io Path
SOCKET_PATH=/socket.io

# Node Environment
NODE_ENV=development

# Storage Configuration
# Set to 'true' to use SQLite for persistent storage
# Set to 'false' or leave unset to use in-memory storage (default)
USE_SQLITE=false

# SQLite Database Path (used when USE_SQLITE=true)
# DB_PATH=./data/mochat.db

# Authentication Configuration
# ⚠️  WARNING: Only use DISABLE_AUTH=true for local development/debugging!
# Setting this to true will bypass ALL authentication checks (both API and Socket.io)
# This is useful for debugging connection issues but should NEVER be used in production
DISABLE_AUTH=false
